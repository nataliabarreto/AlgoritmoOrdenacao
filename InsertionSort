#include <stdio.h>
main()
{
int i,j,aux;
int a[10]={5,2,9,6,3,4,8,10,7,1};
for(i=1;i<10;i++)
{
	aux=a[i];
	while(i>0 && a[i-1]>aux) {
		j=a[i];
		a[i]=a[i-1];
	   a[i-1]=j;
	   --i;
	}
}
int k;
for(k=0;k<10;k++)
{printf("%d	", a[k]);}
printf("\n");
	}
